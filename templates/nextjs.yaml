name: Next.js
version: "1.0"
description: React framework with SSR/SSG
icon: "⚡"
category: web
tags:
  - react
  - nextjs
  - ssr
framework: nextjs
dockerfile: Dockerfile
env:
  PORT: "3000"
ports:
  - 3000
volumes:
  - name: app
    mountPath: /app
